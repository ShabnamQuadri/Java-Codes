public class operators {
  public static void main(String[] args) {
    // Arithmetic Operators
    // int a = 20, b = 54;
    // System.out.println("add " + (a + b));
    // System.out.println("sub " + (a - b));
    // System.out.println("multiply " + (a * b));
    // System.out.println("divide " + (a / b));
    // System.out.println("remainder " + (a % b));
    // Relational Operators
    int p = 10, q = 15, r = 5;
    // System.out.println(p == q);// false
    // System.out.println(p != q);// true
    // System.out.println(p <= q);// true
    // System.out.println(p >= q);// false
    // System.out.println(p > q);// false
    // System.out.println(p < q);// true
    // Logical Operators
    // and (&&) - both needs to be true for execution
    // and (||) - anyone needs to be true for execution
    // and (!) - not of equal to
    // System.out.println((p > q) && (p > r)); // false
    // System.out.println((p > q) || (p > r));// true
    // System.out.println((p < q) && (p > r));//true
    // System.out.println(!(p > q) && (p > r));//true
    // Unary Operator ++num, --num
    // Bitwise operator
    // bitwise OR
    // 09-1001
    // 10-1010
    // 09|10
    // ans-1011
    // bitwise AND -&
    // bitwise XOR -^ same bits toh 0,diff bits hai toh 1
    // bitwise complement - ~
    // left shift << (a<<b = a*2^b)
    // syntax : 9<<2 (9 needs to be shift left by 2)
    // right shift >> (a<<b = a/2^b)

  }
}
